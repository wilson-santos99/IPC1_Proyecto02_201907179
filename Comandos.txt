# comando para iniciar un proyecto nuevo en node (-y  es para dar si a todo)
npm init -y

#comando para instalar dependencias antes de ejecutar un proyecto
npm install

# para instalar un paquete se usa el comando (alguna herramienta que se quiera utilizar)
npm install nombre_paquete    

#git ignore para 
